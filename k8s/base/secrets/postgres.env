# Placeholder values — overridden by the overlay's secretGenerator (behavior: replace).
# Real values live in overlays/<env>/secrets/postgres.env (gitignored).
POSTGRES_DB=stalwart
POSTGRES_USER=stalwart
POSTGRES_PASSWORD=changeme-overridden-by-overlay
