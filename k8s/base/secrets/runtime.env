# Placeholder values — overridden by the overlay's secretGenerator (behavior: replace).
# Real values live in overlays/<env>/secrets/runtime.env (gitignored).
STALWART_PG_PASSWORD=changeme-overridden-by-overlay
STALWART_GCS_SECRET_KEY=changeme-overridden-by-overlay
